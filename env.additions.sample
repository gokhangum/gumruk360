# Add these to your .env.local (or update existing)
# Map hosts to tenant language codes
TENANT_HOST_MAP=gumruk360.com:tr,tr.easycustoms360.com:en,127.0.0.1:en,localhost:tr

# Default if no host match (kept as TR)
DEFAULT_TENANT_CODE=tr
